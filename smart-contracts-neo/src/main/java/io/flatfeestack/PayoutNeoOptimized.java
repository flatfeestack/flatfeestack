package io.flatfeestack;

import io.neow3j.devpack.annotations.Permission;
import io.neow3j.devpack.constants.NativeContract;

@Permission(nativeContract = NativeContract.CryptoLib)
@Permission(nativeContract = NativeContract.GasToken)
public class PayoutNeoOptimized {

    // TODO: 20.10.21 Summarize class implementation after evaluation -> include most optimized methods.

}
