package main

// Tests

// CloneOrUpdateRepository

/*
	Integration method that switches between cloning an updating
*/

// CloneRepository

/*
	Method, that simply uses go-git. Since we don't test git nor go-git, this is not tested
*/

// UpdateRepository

/*
	Method, that simply uses go-git. Since we don't test git nor go-git, this is not tested
*/
