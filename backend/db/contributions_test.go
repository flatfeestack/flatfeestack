package db

import "testing"

func TestContributionInsert(t *testing.T) {
	setup()
	defer teardown()

	//userSponsor := insertTestUser(t, "sponsor")
	//userContrib := insertTestUser(t, "contrib")
	//r := insertTestRepo(t)
	//insertPaymentCycle(t)

	//InsertContribution(userSponsor.Id, userContrib.Id, r.Id)

}
